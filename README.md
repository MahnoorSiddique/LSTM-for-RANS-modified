# LSTM-for-RANS-modified
The project implements and evaluates an LSTM neural network pipeline for predicting Reynolds shear stress profiles. Motivated by the research of Pasinato (2024), which demonstrated that LSTM networks can  effectively replace traditional RANS turbulence models, this project reproduces and extends those concepts in a structured ML pipeline.
